class BottomNavbarItemModel {
  final String label;
  final String icon;
   bool isSelected;

  BottomNavbarItemModel({
    required this.label,
    required this.icon,
    required this.isSelected,
  });
}
